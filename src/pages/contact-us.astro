---
export const prerender = true;

// Types
import type { TranslationData } from "../../interfaces/common/types";

// Stores
import { getTranslations } from "../../stores/addTranslations";

// Components
import MainLayout from "../layouts/MainLayout.astro";
import ContactUsPage from "../components/contact-us/contactUs.svelte";

// Set Up Translation Store
const translationStore: TranslationData = getTranslations.get()

// TODO
let seo = {
  pageTitle: "Contact Us",
  metaDescription: "Contact Us",
  url: "",
};
---

<MainLayout seoData={seo}>
  <ContactUsPage translations={translationStore} client:load />
</MainLayout>
