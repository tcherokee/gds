---
import { getTranslations } from "../../../utils/api-requests";
import FavouriteGamesPage from "../../components/dashboard/favouriteGames.svelte";
import DashboardLayout from "../../layouts/DashboardLayout.astro";

const translationStore = await getTranslations();
---

<DashboardLayout {translationStore}>
  <FavouriteGamesPage translations={translationStore ?? {}} client:load />
</DashboardLayout>
