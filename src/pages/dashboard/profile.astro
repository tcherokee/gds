---

// Types
import type { TranslationData } from "../../../interfaces/common/types";

// Stores
import { getTranslations } from "../../../stores/addTranslations";

// Components
import DashboardLayout from "../../layouts/DashboardLayout.astro";
import ProfilePage from "../../components/dashboard/profile.svelte";

// Set Up Translation Store
const translationStore: TranslationData = getTranslations.get()

---

<DashboardLayout>
  <ProfilePage translations={translationStore} client:load />
</DashboardLayout>
