---

// Types
import type { TranslationData } from "../../../interfaces/common/types";

// Stores
import { getTranslations } from "../../../stores/addTranslations";

// Components
import MainLayout from "../../layouts/MainLayout.astro";
import RegisterForm from "../../components/auth/registerForm.svelte";

// Set Up Translation Store
const translationStore: TranslationData = getTranslations.get()

// TODO
let seo = {
  pageTitle: "Registration",
  metaDescription: "Registration Description",
  url: "",
};
---

<MainLayout seoData={seo}>
  <RegisterForm translations={translationStore} client:only="svelte" />
</MainLayout>
