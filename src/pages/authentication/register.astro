---
// Components
import MainLayout from "../../layouts/MainLayout.astro";
import RegisterForm from "../../components/auth/registerForm.svelte";
import { getTranslations } from "../../../utils/api-requests";

const translationStore = await getTranslations();

// TODO
let seo = {
  pageTitle: "Registration",
  metaDescription: "Registration Description",
  url: "",
};
---

<MainLayout seoData={seo}>
  <RegisterForm translations={translationStore ?? {}} client:load />
</MainLayout>
