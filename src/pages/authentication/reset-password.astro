---
// Components
import MainLayout from "../../layouts/MainLayout.astro";
import ResetPasswordForm from "../../components/auth/registerForm.svelte";
import { getTranslations } from "../../../utils/api-requests";

const translationStore = await getTranslations();

// TODO
let seo = {
  pageTitle: "Reset Password",
  metaDescription: "Reset Password Description",
  url: "",
};
---

<MainLayout seoData={seo}>
  <ResetPasswordForm translations={translationStore ?? {}} client:load />
</MainLayout>
