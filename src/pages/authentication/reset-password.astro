---
import MainLayout from "../../layouts/MainLayout.astro";
import { get } from "svelte/store";
import { getTranslations } from "../../../stores/addTranslations";
import ResetPasswordForm from "../../components/auth/register-form.svelte";

const translationStore: any = get(getTranslations);

// TODO
let seo = {
  pageTitle: "Reset Password",
  metaDescription: "Reset Password Description",
  url: "",
};
---

<MainLayout seoData={seo}>
  <ResetPasswordForm client:only="svelte" />
</MainLayout>
