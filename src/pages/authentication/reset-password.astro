---

// Types
import type { TranslationData } from "../../../interfaces/common/types";

// Stores
import { getTranslations } from "../../../stores/addTranslations";

// Components
import MainLayout from "../../layouts/MainLayout.astro";
import ResetPasswordForm from "../../components/auth/registerForm.svelte";

// Set Up Translation Store
const translationStore: TranslationData = getTranslations.get()

// TODO
let seo = {
  pageTitle: "Reset Password",
  metaDescription: "Reset Password Description",
  url: "",
};
---

<MainLayout seoData={seo}>
  <ResetPasswordForm translations={translationStore} client:only="svelte" />
</MainLayout>
